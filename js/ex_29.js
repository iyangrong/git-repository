/*var score = Math.round(Math.random() * 100);
if(score >= 90){
    grade = "A";
} else if (score >= 80) {
    grade = "B";
} else if (score >= 70){
    grade = "C";
} else if (score >= 60) {
    grade = "D";
} else {
    grade = "F";
}
alert(score + " is a " + grade);*/
/*var pennies = prompt("Enter a number of cents");
if (isNaN(pennies)) {
    alert("This is not a number.");
} else if (pennies < 0) {
    alert("That number is too small.");
} else if (pennies > 9E15) {
    alert("That is number is too large.");
} else if (pennies % 1 !== 0){
    alert("I can only handle whole numbers.");
} else {
    var dollars = Math.floor(pennies / 100);
    var cents = pennies % 100;
    if (dollars > 1 && cents > 1) {
        alert("That's " + dollars + " dollars and " + cents + " cents");
    }
    else if (dollars > 1 && cents === 1) {
        alert("That's " + dollars + " dollars and " + cents + " cent");
    }
    else if (dollars === 1 && cents > 1) {
        alert("That's " + dollars + " dollar and " + cents + " cents");
    }
    else {
        alert("That's " + dollars + " dollar and " + cents + " cent");
    }
}*/
/*var values = [5, 7, 3, 1, 4];
var zeroCount = 0;
var x = +prompt("Enter a number");
if (x > 0) {
    values.push(x);
} else if (x < 0) {
    values.unshift(x);
} else if (x === 0) {
    ++zeroCount;
}
alert(zeroCount);
alert(values);*/
/*alert(25 > -7 ? null : "next");*/
/*var latitude = +prompt("Enter a latitude");
var hemisphere;
if (latitude > 0) {
    hemisphere = "north";
} else if (latitude < 0){
    hemisphere = "south";
} else if (latitude === 0) {
    hemisphere = "equator";
}
alert(hemisphere);*/
 